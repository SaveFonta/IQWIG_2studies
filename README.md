# To Run
Create an `Input` folder in the main directiory and add there your data, with the name: `IQWiG-MA-Datenbank_Stand2024.xlsx`
Then create the `Ouput` folder. 

Then run each file following the numerical order (no necessary to run the files starting with `00.` ) 


Note: the `master_for_generating.R` creates a singular report for EACH meta Analisis
